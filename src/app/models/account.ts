export interface Account {
  id: number;
  phoneNumber: string;
  firstName: string;
  lastName: string;
  accountNumber: string;
  email: string;
  userId: string;
  ticketNumber: string;
  streetAddress: string;
  apt: string;
  zip: string;
  error?: string;
}
